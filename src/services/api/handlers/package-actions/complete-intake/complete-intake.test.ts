import { describe, it, expect } from "vitest";

// this is a placeholder. i will rewrite these shortly
describe("example test", () => {
  it("should equal 1", () => {
    expect(1).toEqual(1);
  });
});