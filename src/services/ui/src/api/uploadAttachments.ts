import { MakoAttachment } from "shared-types";

export const grabPreSignedURL = () => {
  // TODO: Grab URL for uploading to S3 bucket
};
export const uploadAttachments = (attachments: MakoAttachment[], url: URL) => {
  // TODO: S3 bucket file upload
};
