import { test } from "@playwright/test";

test.describe.skip("Respond to RAI", async () => {
  // comment this out until we need it
  // test.beforeAll();
  test("Submit a Respond to RAI for a Medicaid SPA", () => {});
});
