import { test } from "@playwright/test";

test.describe.skip("Subsequent Documents", async () => {
  // comment this out until we need it
  // test.beforeAll();
  test("Submit a subsequent documents for a Medicaid SPA", () => {});
});