import { test } from "@playwright/test";

test.describe.skip("Enable RAI Response withdraw", async () => {
  // comment this out until we need it
  // test.beforeAll();
  test("Perform enable RAI response withdraw for a Medicaid SPA", () => {});
});
