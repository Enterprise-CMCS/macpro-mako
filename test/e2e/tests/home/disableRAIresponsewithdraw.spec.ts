import { test } from "@playwright/test";

test.describe.skip("Disable RAI Response withdraw", async () => {
  // comment this out until we need it
  // test.beforeAll();
  test("Perform disable RAI response withdraw for a Medicaid SPA", () => {});
});
