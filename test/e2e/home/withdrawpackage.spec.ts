import { test } from "@playwright/test";

test.describe.skip("Withdraw package", async () => {
  // comment this out until we need it
  // test.beforeAll();
  test("Perform withdraw package action for a Medicaid SPA", () => {});
});