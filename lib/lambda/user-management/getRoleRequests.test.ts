// import { APIGatewayEvent } from "aws-lambda";
// import { describe, expect, it } from "vitest";

// import { getRoleRequests } from "./getRoleRequests";

// describe("getRoleRequests", () => {
//   it("should error if user isn't authenticated", async () => {
//     const event = {} as APIGatewayEvent;
//   });
//   it("should error if user doesn't have roles", async () => {
//     const event = {} as APIGatewayEvent;
//   });
//   it("should get all state role requests if user is a CMS role approver", async () => {
//     const event = {} as APIGatewayEvent;
//   });
//   it("should get corresponding state role requests if user is a state system admin", async () => {
//     const event = {} as APIGatewayEvent;
//   });
// });
