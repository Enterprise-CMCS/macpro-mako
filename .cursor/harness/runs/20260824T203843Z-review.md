# Harness review: oy2-40481-smart-outbound-events

- **written:** 2026-08-24T20:38:43Z
- **target:** branch `oy2-40481-smart-outbound-events`, spec slug `oy2-40481-smart-outbound-events`
- **scope:** uncommitted SMART outbound Kafka ingest + `MSP_MANUAL_RECORD_CREATED` (includes collision merge of `correlationId` + `spaWaiverId` only)
- **spec:** `.cursor/harness/specs/oy2-40481-smart-outbound-events.md`
- **PlanPacket:** `.cursor/harness/runs/20260824T171430Z-plan-packet.json`
- **checklist:** `~/.cursor/harness/verification-checklist.md` (repo `.cursor/harness/` has no copy)
- **waiver:** `.cursor/harness/runs/20260824T193733Z-test-tsc-waiver.md`
- **prior reviews:** `.cursor/harness/runs/20260824T191630Z-review.md` (paused), `.cursor/harness/runs/20260824T194452Z-review.md` (blocked)
- **final result:** `approved`

## Waiver

User-confirmed 2026-08-24: repo-wide `bun run test-tsc` exit 0 is not blocking. Blocking typecheck is `bun run build`. Slice still fails if `test-tsc` reports errors in SMART-changed production paths. PlanPacket T2/T12 still list `test-tsc` exit 0; the plan was not edited.

Spec decision 11 supersedes PlanPacket T5/T6 “zero OpenSearch mutations”: collision logs, then merges only `correlationId` and `spaWaiverId`.

## Verifier

- **attempts:** 2 (this review)
- **latest status:** `pass`
- **commands:** T12 focused SMART/regression/stack/opensearch/setupIndex tests; isolation `rg`; `email.ts` / `itemExists` / `checkIdentifierUsage` / `search` production-file diffs; `bun run lint`; `bun run build`; `bun run test-tsc` (waived exit 0); secrets scan
- **round 1 fail:** `bun run lint` (13 Prettier errors in `reservePackageId.test.ts`, `opensearch-lib.ts`, `opensearch-lib.test.ts`)
- **round 2 pass:** lint, focused tests, build exit 0; `test-tsc` exit 2 with zero errors in SMART production paths

## Critic / fix rounds

1. Critic scoped format-only fix on the three lint-flagged files. Implementer ran Prettier on those files only. No product behavior change.

## Reviewer

- **status:** `approved`
- **actionable findings:** none blocking
- **warnings:**
  1. Collision CloudWatch writes are swallowed after a successful identity merge (audit log can be lost; merge is idempotent).
  2. Transform writes `seatoolStatus` only; `stateStatus`/`cmsStatus` and some event fields remain dropped (low while U2 hide holds).
- **nit:** Confluence fixture email still copied across SMART tests.

Prior blockers from 20260824T194452Z are resolved: VPC/ENI managed policies + stack `ManagedPolicyArns` assertions; OpenSearch/infra errors rethrow; atomic `createItem` + `mergeSmartIdentityFields` (`correlationId` + `spaWaiverId` only).

## Security

- **critical:** none
- **high:** none
- **medium:** none (prior check-then-`doc_as_upsert` overwrite is resolved)
- **residual (not blocking):**
  - Kafka ACL trust for `origin: "SMART"` producers
  - Collision/validation logs may include submitter PII
  - U2 hide is only on `item.ts`; other `getPackage` consumers can still see SMART reservations
  - U1 / U3 / U8 / U11 remain open

## Warnings

- U1, U2, U3, U8, and U11 remain unresolved and still require confirmation before production rollout.
- Root `bun.lockb` is dirty; do not stage incidental lockfile churn.
- PlanPacket still lists `test-tsc` exit 0 and T5/T6 zero-mutation collision; spec + waiver are the later sources of truth.

## Next action

Harness approval is granted. Shipping remains `harness-ship` (commit, push, PR). Confirm U1/U2/U3/U8/U11 before production rollout.
