# Harness review: oy2-40481-smart-outbound-events

- **written:** 2026-08-24T19:16:30Z
- **target:** branch `oy2-40481-smart-outbound-events`, spec slug `oy2-40481-smart-outbound-events`
- **scope:** uncommitted SMART outbound Kafka ingest + `MSP_MANUAL_RECORD_CREATED` (OY2-40481 / OY2-41233)
- **spec:** `.cursor/harness/specs/oy2-40481-smart-outbound-events.md`
- **PlanPacket:** `.cursor/harness/runs/20260824T171430Z-plan-packet.json`
- **checklist:** `~/.cursor/harness/verification-checklist.md` (repo `.cursor/harness/` has no copy)
- **final result:** `paused`

Reviewer and security-reviewer were not run. The review skill requires verifier pass before those agents.

## Verifier

- **attempts:** 3
- **latest status:** `fail`
- **commands:** T12 PlanPacket suite (focused SMART/regression tests, isolation `rg`, production-file `git diff` guards, `bun run lint`, `bun run test-tsc`, `bun run build`, secrets scan)

Round 1 fail: `bun run test-tsc`, `bun run build`.

Round 2 fail: `bun run test-tsc` (~2852 repo-wide errors, zero in SMART paths); first `data.smart-events.test.ts` run had two 15s CDK-synth timeouts (immediate retry 20/20). `bun run build` then passed.

Round 3 fail: only `bun run test-tsc` (exit 2, ~2852 errors, zero in `sinkSmart`, `lib/lambda/smart/`, `lib/stacks/data.ts`, `lib/lambda/item.ts`, or `data.smart-events`). All focused tests, lint, build, isolation, email.ts unchanged, and secrets scan passed.

## Critic / fix rounds

1. Stale `lib/attachment-archive/node_modules` (archiver 7 vs lockfile 8). Implementer ran `bun install` in that directory only. No source edit. Build recovered.
2. File-local Vitest timeout in `lib/stacks/data.smart-events.test.ts` (`vi.setConfig` 60s). Implementer applied that only. Stack tests then passed on the next verifier run.

Both critics classified `bun run test-tsc` as a pre-existing repo-wide baseline (root `tsconfig.json` includes `react-app/**/*` without the `@/*` alias) and forbade chasing it in-slice.

## Reviewer

Not run. Status: n/a. Findings: none.

## Security

Not run. Findings by severity: none recorded.

## Introspection

After the third verifier fail, introspection classified the stall as **policy**, not a SMART-code defect. PlanPacket T2/T12 and the checklist require `bun run test-tsc` exit 0; CI does not run that script and the current root typecheck cannot pass on any commit. Recommended next agent: user.

## Warnings

- U1, U2, U3, U8, and U11 remain unresolved and still require confirmation before production rollout.
- Root `bun.lockb` is dirty (CloudWatch Logs SDK plus possible incidental install churn). Do not stage unrelated lockfile drift.
- `lib/attachment-archive` local `node_modules` refresh is machine-local; it is not a committed fix.

## Next action

Pause for a user decision. Grant a documented, slice-scoped waiver of the `bun run test-tsc` exit-0 gate (justified by zero slice-path errors, passing `bun run build` / focused tests / lint, and CI parity), or change the harness/CI typecheck contract in a separate hygiene ticket. Do not start another verifier/critic/implementer loop until that decision is made. After a waiver or a real typecheck-contract change, re-invoke harness-review so reviewer and security-reviewer can run.
