# Harness review: oy2-40481-smart-outbound-events (post-waiver)

- **written:** 2026-08-24T19:44:52Z
- **target:** branch `oy2-40481-smart-outbound-events`, spec slug `oy2-40481-smart-outbound-events`
- **scope:** uncommitted SMART outbound Kafka ingest + `MSP_MANUAL_RECORD_CREATED`
- **spec:** `.cursor/harness/specs/oy2-40481-smart-outbound-events.md`
- **PlanPacket:** `.cursor/harness/runs/20260824T171430Z-plan-packet.json`
- **checklist:** `~/.cursor/harness/verification-checklist.md`
- **waiver:** `.cursor/harness/runs/20260824T193733Z-test-tsc-waiver.md`
- **prior review:** `.cursor/harness/runs/20260824T191630Z-review.md` (paused on `test-tsc`)
- **final result:** `blocked`

## Waiver

User-confirmed 2026-08-24: repo-wide `bun run test-tsc` exit 0 is not blocking. Blocking typecheck is `bun run build` with no compilation errors. Slice still fails if `test-tsc` reports errors in SMART-changed production paths.

## Verifier

- **attempts:** 3 (from prior review); not re-run after waiver
- **latest status:** treated as **pass-with-waiver**
- **commands that passed:** focused SMART/regression tests, lint, isolation `rg`, production-file diffs, secrets scan, `bun run build`
- **waived:** `bun run test-tsc` exit 0 (~2852 pre-existing errors, zero in SMART production paths)
- **critic/fix rounds:** archive `node_modules` refresh (build); stack-test timeout now 3 minutes in `lib/stacks/data.smart-events.test.ts`

## Reviewer

- **status:** `changes_requested`
- **actionable findings:**
  1. `sinkSmartRole` missing VPC/ENI managed policies — likely CloudFormation create failure.
  2. `sinkSmart` swallows OpenSearch/infrastructure errors and commits the Kafka offset — reservation can be lost.
  3. `reservePackageId` read-then-`doc_as_upsert` can merge SMART fields onto a real package in a race (also the security medium).
  4. Stack tests do not inspect `ManagedPolicyArns`, so ENI coverage cannot fail closed.
  5. Transform sets `seatoolStatus` only; `stateStatus`/`cmsStatus` and some event fields are dropped (low impact while U2 hide holds).
  6. Confluence fixture email duplicated across tests (hygiene, not a secret).

## Security

- **critical:** none
- **high:** none
- **medium:** check-then-`doc_as_upsert` can overwrite or merge a legitimate main-index package (same as reviewer finding 3). Warning; does not block approval by severity rule.
- **other residual:** Kafka ACL trust for `origin: "SMART"`; U2 hide is only on `item.ts`; collision logs may include submitter email.

## Warnings

- U1, U2, U3, U8, U11 remain unresolved before production.
- Root `bun.lockb` is dirty; do not stage incidental lockfile churn.
- PlanPacket was not edited; T2/T12 still list `test-tsc` exit 0. The waiver lives in the spec and the waiver note.

## Next action

Paused for the user. Reviewer `changes_requested` blocks harness approval. Decide whether to fix findings 1–3 (and optionally 4) before another review, or accept residual risk on 3/5/6 in writing. Do not start an implementation loop until that decision. After fixes, re-invoke harness-review. Shipping remains `harness-ship`.
